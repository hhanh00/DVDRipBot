<%= javascript_include_tag :defaults %>

<h1>New disk</h1>

<% form_for(@disk) do |f| %>
  <%= f.error_messages %>

  <%= render(:partial => 'disk_form', :object => f) %>
  
  <p>
    <%= f.submit 'Create' %>
  </p>

<% end %>

<%= link_to 'Back', disks_path %>